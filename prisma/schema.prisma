generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model item {
  id          Int     @id @default(autoincrement())
  name        String?
  description String?
  slug        String  @unique
  long_description String
}
